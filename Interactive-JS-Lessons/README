dependencies:
if you want to run your own instance of backend server:
mongoDB
https://www.mongodb.com/docs/manual/administration/install-community/

to run the front end: 
node.js/NPM package manager

helpful packages:
npm install nodemon -g

open frontend folder in instance of liveserver 
